fetch('public-snapshot.json').then(r=>r.json()).then(data=>{const counts=[['Agents',data.agents.length],['Goals',data.goals.length],['Skills',data.skills.length],['Tasks',data.tasks.length],['Artifacts',data.artifacts.length]];document.querySelector('#summary').textContent='監査可能な作業記録から生成した、機密情報を含まない読み取り専用の状態です。';document.querySelector('#cards').innerHTML=counts.map(([name,count])=>`<article class="card"><b>${count}</b><span>${name}</span></article>`).join('')}).catch(()=>document.querySelector('#summary').textContent='公開スナップショットはまだ生成されていません。');
